# Server
PORT=3000
NODE_ENV=development
HOST=0.0.0.0

# Panel auth (>= 32 chars in production)
BETTER_AUTH_SECRET=change-me-to-a-32-char-random-string

# Trust proxy (Render sets this)
TRUST_PROXY=loopback

# SQLite file location (default: ./data/xrc.db)
DATABASE_PATH=./data/xrc.db
